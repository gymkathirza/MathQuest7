# Plan: Realm isometric companion (0.20.0)

Execute the approved spec `docs/superpowers/specs/2026-08-18-realm-isometric-companion-design.md`.

1. Generate MathQuest-owned isometric SVG art (buildings + pets + skins) + `LICENSE.md`.
2. Extend `rewards.mjs` with `art` paths, `realmArtUrl`, `companionStripView`.
3. Wire `<img>` into My Realm stage/shop; add `#realmCompanion` on practice.
4. Bump version/`sw.js`/docs/smoke; open PR.
