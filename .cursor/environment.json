{
  "name": "MathQuest 7",
  "install": "python3 --version && test -f index.html && test -f sw.js && test -f manifest.webmanifest",
  "terminals": [
    {
      "name": "static-server",
      "command": "python3 -m http.server 8000 --bind 0.0.0.0"
    }
  ],
  "ports": [8000]
}
